# Server list
server_list = ["127.0.0.1"]

# Timeout in millisecond
timeout = 50

# QQ group ID
group_id = "0"

# IP and port of Go-cqhttp HTTP API
api_ip = "127.0.0.1:5700"

# Delay between 2 lookups in second
dalay = 1

# messages send when online and offline, use ~server~ in place of server address
online_massage = "Server [~server~] is online now"
offline_massage = "Server [~server~] is offline now"